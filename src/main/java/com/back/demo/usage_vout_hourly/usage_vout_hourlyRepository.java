package com.back.demo.usage_vout_hourly;
import org.springframework.data.jpa.repository.JpaRepository;

public interface usage_vout_hourlyRepository extends JpaRepository<usage_vout_hourly, Long> {
}
